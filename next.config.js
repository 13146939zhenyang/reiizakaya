/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "izakaya-asset.s3.ap-southeast-2.amazonaws.com"
        ]
    }
}

module.exports = nextConfig
